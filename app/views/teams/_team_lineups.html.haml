.lineup-hint
  - if team.lineups.where(tour_id: active_tour&.id).exists?
    .alert.alert-primary
      %h6 Finish tour ##{active_tour&.number} and wait next
  - elsif active_tour&.set_lineup?
    .alert.alert-warning
      = link_to 'Create lineup', new_team_lineup_path(team), class: 'btn btn-primary active'
      = link_to 'Clone last lineup', team_lineup_clone_path(team, team.lineups.first), class: 'btn btn-primary active'
  - else
    .alert.alert-warning
      %h6 Ask Admin to activate tour

- team.lineups.each do |lineup|
  = render 'lineup', lineup: lineup
